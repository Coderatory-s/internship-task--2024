// import fe
// const ActiveGroups = () => {
//   const { data: groups, isLoading } = useQuery("activeGroups", fetchActiveGroups);

//   if (isLoading) return <p>Loading...</p>;

//   return (
//     <div>
//       <h4>Active Groups</h4>
//       {groups.map((group) => (
//         <div key={group.id} className="mb-2">
//           <p>{group.name}</p>
//         </div>
//       ))}
//     </div>
//   );
// };

// export default ActiveGroups;
